把小写字母转化为大写：{字符串}.upper()

字符串分割：{字符串}.split(" ")

颠倒列表：{列表}[::-1]

用空格分割输出：print({要输出内容},end=" ")

控制循环次数：for i in range({数字})

死循环：
while True:
    try:
    except:

给列表新增元素：{列表}.append

创建无序不重复元素集：set({列表})

